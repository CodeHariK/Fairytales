import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Temporarily disabled due to Turbopack compatibility issues
  // reactCompiler: true,
};

export default nextConfig;
